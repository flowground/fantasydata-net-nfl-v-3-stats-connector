{
    "type": "object",
    "properties": {
        "format": {
            "required": true,
            "type": "string",
            "enum": [
                "XML",
                "JSON"
            ],
            "default": "XML"
        },
        "minutes": {
            "required": true,
            "type": "string"
        }
    }
}