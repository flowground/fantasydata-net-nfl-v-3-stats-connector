{
    "type": "object",
    "properties": {
        "format": {
            "required": true,
            "type": "string",
            "enum": [
                "XML",
                "JSON"
            ],
            "default": "XML"
        },
        "scoreid": {
            "required": true,
            "type": "string"
        }
    }
}